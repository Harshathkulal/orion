export interface TextContentProps {
    answer: string;
    loading: boolean;
    initial: boolean;
  }
  
 export interface CodeBlockProps {
    className?: string;
    children?: React.ReactNode;
  }
  